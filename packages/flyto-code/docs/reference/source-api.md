# Warroom CE Frontend Source Reference

The upstream private-source inventory is not copied into the generated CE
package because it includes declarations that are intentionally excluded from
the community source boundary. Use the generated
[Warroom source reference](../../../../docs/reference/README.md), which is
rebuilt from the exported tree and links every included frontend declaration to
its exact source line.

/**
 * Link adapter — product code imports the router Link from here, not from
 * `@fuse/*` directly (arch Phase 4: keep Fuse swappable behind adapters).
 */
export { default } from '@fuse/core/Link'

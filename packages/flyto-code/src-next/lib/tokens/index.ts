/**
 * @lib/tokens — canonical design tokens for the workspace UI.
 *
 * One-stop import for severity / grade / importance / kind palettes
 * + heat-map intensity ramp. Replaces the per-compound inline tone
 * tables that drifted over time. See `severity.ts` for the full
 * type table.
 */

export * from './severity'

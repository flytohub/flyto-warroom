# Flyto2 Warroom CE frontend local development config.
# Docker Compose uses the same-origin nginx proxy and needs no frontend secret.
VITE_AUTH_MODE=local_jwt
VITE_ENGINE_PROXY_TARGET=http://localhost:8080

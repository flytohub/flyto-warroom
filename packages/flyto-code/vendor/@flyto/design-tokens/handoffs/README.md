# Handoffs

Use `_registry.md` to index dated cross-session handoffs. Record verified facts,
remaining work, commands already run, release state, and consumer risks. Do not
store credentials or customer data here; current repository status belongs in
`STATE.md`, while durable behavior belongs in `docs/`.

# public

Static assets copied into the Vite build output.

The `i18n/` subtree is populated from the sibling `flyto-i18n` repository in
development and CI. Do not hand-edit generated translation bundles here; update
the shared i18n repository and run the sync script instead.
